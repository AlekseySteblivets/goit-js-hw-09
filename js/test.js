import cards from './practics.js';
const listEl = document.createElement('ul');
listEl.classList.add('cars-list');
document.body.prepend(listEl);

const makeCards = ({ name, url, id, description }) => {
    const itemCarsEl = document.createElement('li');

    // return cards.map(card = > )
}


    // name: 'BMW',
    //     url: 'https://a.d-cd.net/8e459a5s-960.jpg',
    //         id: 1,
    //             description: 'BMW'